const initialState = {
  text: "Companies like Google, Facebook, and Microsoft.."
};

const reducer = (state = initialState, action) => {
  const newState = { ...state };

  return newState;
};

export default reducer;
